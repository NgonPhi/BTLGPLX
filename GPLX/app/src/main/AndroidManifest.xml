<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ngonphikp.gplx">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Activity.TCLActivity"></activity>
        <activity android:name=".Activity.MTActivity" />
        <activity android:name=".Activity.CCSActivity" />
        <activity android:name=".Activity.SetUpGPLXActivity" />
        <activity android:name=".Activity.TSHActivity" />
        <activity android:name=".Activity.HLTActivity" />
        <activity android:name=".Activity.BBDBActivity" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>